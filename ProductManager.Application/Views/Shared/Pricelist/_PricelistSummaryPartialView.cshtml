@* @model ProductManager.Application.Models.DBEntities.Pricelist
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>
            @Model.PricelistName
            @if (Model.PricelistID != 0)
            {
                <span class="badge rounded-pill bg-primary text-white" style="float:right">
                    <small>@Model.PricelistPrice ₽</small>
                </span>
                <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="@("#updatePricelist_" + Model.PricelistID)" asp-controller="Pricelist" asp-action="UpdatePricelist">
                    Редактировать @Model.PricelistName
                </button>
                <partial name="../Shared/Pricelist/_PricelistUpdatePartialView" model="new PricelistViewModel(Model, ViewBag.Categories)" />
                <button type="button" class="btn btn-outline-success" data-bs-toggle="modal" data-bs-target="@("#pricelistDetails_" + Model.PricelistID)" asp-controller="Pricelist" asp-action="PricelistDetails">
                    Подробнее о @Model.PricelistName
                </button>
                <partial name="../Shared/Pricelist/_PricelistDetailsPartialView" model="new PricelistViewModel(Model, ViewBag.Categories)" />
                <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="@("#deletePricelist_" + Model.PricelistID)" asp-controller="Pricelist" asp-action="DeletePricelist">
                    Удалить @Model.PricelistName
                </button>
                <partial name="../Shared/Pricelist/_PricelistDeletePartialView", model="Model" />
            }
        </h4>
    </div>
    <div class="card-text p-1">@Model.PricelistDescription</div> *@